%h2 SNPs

- form_tag url_for(:snps_index), method: 'get' do
  %p
    = label_tag 'rs_number'
    = text_field_tag 'rs_number', id: 'rs_number', value: params[:rs_number]
  %p
    = submit_tag :Search

- if @snp
  #snp
    %p.rs_number= @snp.rs_number
